{
  "version": 2,
  "buildCommand": "cd frontend && npm install --omit=optional --no-audit --no-fund && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
} 