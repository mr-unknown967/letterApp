{
  "version": 2,
  "buildCommand": "npm install --omit=optional --no-audit --no-fund && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "nodeVersion": "18.x",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
} 